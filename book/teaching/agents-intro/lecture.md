---
title: Introduction to AI agents for the GAIA team
short_title: Agents lecture
description: A 45-minute seminar on what an AI agent is, what it is made of, how we already use one in GAIA, and what it will take to run agents reproducibly across the collaboration.
---

(agents-intro-lecture)=

:::{note}
**Status.** Draft outline with speaker notes, prepared 17 September 2026 for the GAIA CSSI
team. Lecture date: TODO. Companion tutorials are stubs, to be filled in with UW eScience
(see [tutorial 1](tutorials/01-anatomy-of-an-agent.md) and the
[compute requirements](compute-requirements.md) working document). The slide deck is
rendered from `slides/agents-intro.qmd` next to this page and published at
[/book/slides/agents-intro/agents-intro.html](https://gaia-hazlab.github.io/book/slides/agents-intro/agents-intro.html);
this page is the source of truth for the spoken notes. Everything marked
**TODO** is a fact or artefact not yet verified from a source; nothing in those slots has
been estimated.
:::

## How to read this page

Each section below is one segment of the lecture. The heading carries the minute range.
The diagrams are generated by `slides/figscripts/make_diagrams.py` into `book/img/` and are
shared with the slide deck.
Under it, *Slides* lists the slides to make, one line each, and *Speaker notes* is what to
say, written to be read aloud or paraphrased. *Timing* flags where the plan is tight.

The examples come from three places: this repository (its `CLAUDE.md`, `AGENTS.md`,
skills and persona agents), the blog *Agents for Academic Practice* [@denolle2026blog],
and the UW eScience workshop *Coding with AI Agents* [@escience2026codingagents]. Where an
example is drawn from a source outside the repository, the citation says so. Where a
story has been told in the group but not written down, the slot is a TODO.

## 0 to 1 min. Models, prompts, and the word "agent"

**Slides**

1. Title slide. *AI agents for GAIA: what they are, what they are made of, how we run them.*
2. Three columns: *open-weight model*, *private model*, *agent*.

**Speaker notes**

Three words need fixing before anything else.

A *model* is a function from a token sequence to a probability distribution over the next
token. An *open-weight* model is one whose parameters you can download and run on your own
hardware. HazEvalHub's first prototype ran two such models locally, a 7 billion parameter
Qwen and an 8 billion parameter Llama, and scored them next to cloud models
([HazEvalHub](../../chapters/hazevalhub.md)). A *private* model is one you reach only
through a vendor's API. You never see the weights, you pay per token, and the vendor can
change the model under you. Most of what we will talk about today applies to both. The
differences that matter for us are cost, data governance, and reproducibility, and we come
back to those at the end.

"Writing a prompt" means placing text in the model's context window and asking for a
continuation. Nothing else happens. The model does not read a file, run code, or remember
the previous conversation unless that text is placed in front of it again. A chat interface
hides this by re-sending the transcript on every turn.

An *agent* is a model wrapped in a loop that lets it act. The loop gives the model tools,
executes the tool calls the model emits, and feeds the results back into the context. The
model is still only predicting tokens. What changed is that some of those tokens are now
commands, and something runs them.

**Timing.** One minute is not enough for three definitions and a distinction. Expect this
to take two to three minutes, and take them from segment 6 to 14, which has slack.

## 1 to 6 min. Agents versus chatbots: the loop

**Slides**

3. Figure: chat session, fixed script, agent loop side by side ([](#fig-agent-loop)).
4. The four verbs: *gather context, act, verify, repeat*.
5. What differs from chat: the model closes the loop. What differs from a script: the
   model chooses the next step.
6. The division of labour we keep: goal, rules and stop condition are human; the
   iterations are the agent's [@denolle2026three].

```{figure} ../../img/agent-loop.svg
:name: fig-agent-loop
:width: 100%

Three ways to put a language model to work. In a chat session (A) the human closes every
loop. In a fixed workflow script (B) every branch was written in advance. In an agent loop
(C) the model reads the result of its last action and decides the next one; the human sets
the goal, the rules, and the stop condition.
```

**Speaker notes**

The figure has three panels. Walk them left to right.

Panel A is a chat session. You ask, the model answers, you read the answer, you decide what
to ask next. The model never touches anything. Every loop is closed by you, and the quality
of the result is bounded by your patience for closing loops.

Panel B is the workflow script most of us already write: a Snakemake rule, a GitHub Action,
a shell script that downloads, converts and plots. It is deterministic, which is its virtue.
Every branch was written in advance, which is its limit. When the input is not what the
author expected, the script fails, and a person reads the log.

Panel C is the agent loop. The model is given a goal and a set of tools. It *gathers
context*: reads the files, lists the directory, fetches the issue. It *acts*: runs a
command, edits a file, calls an API. It *verifies*: reads the test output, checks the
diff, re-reads the file it just wrote. Then it *repeats* until it judges the goal met or
hits a stop condition. The difference from panel A is that the model closes the loop. The
difference from panel B is that the model chooses the next step from what it just
observed, so an unexpected state is a decision rather than a crash.

Two consequences follow, and both matter for how we run these things in GAIA. First, the
loop is only as good as the *verify* step. An agent that cannot run the tests, or is not
told to, will report success on the strength of having edited a file. Second, the loop has
no natural end. It stops when the model says it is done, when a budget runs out, or when a
person kills it. We will see all three today.

The division of labour we have settled on in the group is written up in the June blog post
[@denolle2026three]: scientific direction, judgement of rigour, and the check against
common sense stay human. Literature search, pipeline construction, method implementation,
and the first pass at critique can be handed to agents. The post's phrasing is that the
scarce resource is no longer the ability to do the work but the human bandwidth to check
it. Hold that thought until the guardrails segment.

**Timing.** Five minutes is right if the figure does the work. Do not narrate the four
verbs twice.

## 6 to 14 min. The assembled pieces

**Slides**

7. The stack, bottom to top: model, harness, context file, tools, skills, subagents, MCP.
8. The context file, using this repository's own `CLAUDE.md` and `AGENTS.md`.
9. Tools: shell, file read and write, web fetch, GitHub, a Python interpreter.
10. Skills: a folder with a `SKILL.md`, using `.claude/skills/plain-voice` as the example.
11. Subagents: `.claude/agents/gaia-review-*.md`, ten persona reviewers.
12. MCP: one interface for many tool servers. Diagram: agent, MCP client, three servers.
13. Anatomy of one agent: a persona reviewer, its references and rubric ([](#fig-agents-anatomy)).
14. Orchestrator versus specialist: ten reviewers, one rubric, one synthesis
    ([](#fig-agents-orchestrator)).
15. Keep the standing files short. Why, and what "short" buys.
16. The context window: what is actually in it, and what happens when it fills
    ([](#fig-agents-context-window)).

```{figure} ../../img/agents-stack.svg
:name: fig-agents-stack
:width: 100%

The assembled pieces. Left: loaded every turn. Centre: the harness, its window and its loop.
Right: called on demand. Bottom: the model, which only ever sees tokens.
```

**Speaker notes**

*The context file.* Every coding agent looks for a standing instruction file in the
repository root. Claude Code reads `CLAUDE.md`; the cross-vendor convention is
`AGENTS.md`. Ours is worth showing because of what it is not. `CLAUDE.md` is five lines
that point at `AGENTS.md`. `AGENTS.md` fits on one screen: what the repo is, the layout,
the build commands, five conventions, and one paragraph about persona reviews. It does not
explain MyST, does not restate the contributing guide, and does not tell the agent how to
write Markdown. It tells the agent where the table of contents lives and which directory
never to edit. That is the job: the facts an experienced collaborator would need on day
one and would not find by reading the code.

The March blog post [@denolle2026beforeprompt] frames this as three layers: a knowledge
anchor (the documents the agent is grounded in), standing instructions (the rules file),
and the situated prompt (this task, this audience). The post's claim is that the first two
layers, not the prompt, determine output quality, and that the instructions are a living
document rather than a one-time setup. Ours has been rewritten several times since June.

*Tools.* A tool is a function the harness exposes to the model with a name, a description,
and a JSON schema for its arguments. The model emits a call; the harness runs it and
returns the result as text. The core set for research work is small: read and write files,
run a shell command, fetch a web page, search the web, and call the GitHub API. A Python
interpreter is just the shell tool with `python` in front. Everything the agent does to the
world goes through this interface, which is why the permission model lives here too: each
tool call can be allowed, denied, or gated on a prompt to the user.

*Skills.* A skill is a folder with a `SKILL.md` and any reference files it needs. The
front matter carries a name and a one-line description; the body carries the instructions.
The harness reads the descriptions at startup and loads the full body only when a task
matches, so a skill costs almost nothing until it is used. This repository ships one,
`plain-voice`, which strips language-model vocabulary out of prose
(`.claude/skills/README.md`). We will build a second one live in segment 24 to 33.

*Subagents.* A subagent is an agent launched by an agent, with its own fresh context
window, its own tool set, and a single task. When it finishes, only its final report comes
back to the parent. This repository defines ten of them under `.claude/agents/`: reviewer
personas (a climate-risk CTO, a foundation program officer, a first-year PhD student, and
so on) that each read the live site and file a structured review under `review-logs/`.
Two things to notice. Each persona runs in isolation, so ten reviews cost ten context
windows rather than one enormous one. And the output is a file with a date on it, which is
the beginning of an audit trail.

*Anatomy of one agent, and orchestrator versus specialist.* The persona reviewers are the
best in-house example of a specialised agent, because everything that makes one is a file
you can open. The agent definition carries a tools allow-list and points at three files it
reads at run time: the persona's own `SKILL.md` (checks in order, weights, vocabulary limits,
a signature question), the shared `method.md` (what to open, a 30-minute timebox, read no
other review), and the shared `rubric.md` (eight dimensions, a four-level severity scale,
evidence rules, and the exact report format). The output is one dated file under
`review-logs/`. Ten of these run independently against the same rubric and a synthesis
step merges them, keeping convergent findings, keeping divergent ones divergent, and
reporting scores as a table rather than an average. That split is the general pattern: the
orchestrator holds a broad goal and only the reports; the specialist holds the whole input
for a bounded time and writes one structured file.

```{figure} ../../img/agents-anatomy.svg
:name: fig-agents-anatomy
:width: 100%

One specialised agent as this repository defines it: the definition file, the shared
references and rubric it reads at run time, the timeboxed loop, and the one file it writes.
```

```{figure} ../../img/agents-orchestrator-vs-specialist.svg
:name: fig-agents-orchestrator
:width: 100%

Orchestrator versus specialist. Ten personas run in parallel and in isolation against one
shared rubric; a synthesis step merges the ten reports.
```

*MCP.* The Model Context Protocol is the interface through which an agent discovers and
calls tools that live outside the harness: a web search service, a GitHub server, a
calendar, a database, a Slack workspace. The agent is the client; each service runs a
server that advertises its tools with the same name-description-schema triple as a native
tool. The practical effect is that adding a capability to an agent is a configuration
line, not a code change. The risk is the same as the benefit: every server you connect
adds tool descriptions to the context and adds a party whose output the model will treat
as data it should act on. TODO: link the protocol specification from a fetched page
rather than from memory.

*Keep the standing files short.* The context file, the skill descriptions, and the tool
schemas are loaded into the window before the task begins, on every turn. A long
`AGENTS.md` is paid for on every call, competes with the task's own material for the
model's attention, and buries the three rules that matter under thirty that do not. The
translator project made the same choice for its discipline summaries: about 350 words
each, so that baseline awareness does not dominate the window
([Gaia Translator](../../chapters/gaia-translator.md)). The rule of thumb we use: if a
sentence in the context file would not change what the agent does, delete it.

*The truth about the context window.* The window is finite. It contains, in order, the
system prompt, the context file, the tool and skill descriptions, and then the running
transcript: your prompts, the model's replies, every tool call and every tool result.
Tool results are the part people forget. A single `cat` of a large log or a directory
listing of a data lake can consume more of the window than the entire conversation so far.
When the window fills, the harness compacts: it summarises the older transcript and
continues from the summary. That summary is lossy. A rule you stated in the first prompt
may not survive into the summary; a file the agent read at minute two may have to be read
again. There is also evidence that models attend less reliably to material in the middle
of a long context than at either end. TODO: cite the source for that claim from a fetched
paper rather than from memory, or drop the sentence.

```{figure} ../../img/agents-context-window.svg
:name: fig-agents-context-window
:width: 100%

What is in the window, which part is re-sent every turn, which part is compacted, and what
compaction loses.
```

The consequences for practice are direct. Put invariant rules in the context file, not in
the prompt, because the context file is re-read and the prompt is compacted. Use subagents
to isolate reads of large material. Start a new session for a new task rather than
continuing a long one. And when an agent that was behaving well starts to drift, suspect
the window before suspecting the model.

**Timing.** Eight minutes for seven pieces and two diagrams is tight, and the slack has
already been lent to segment 0 to 1. If it runs long, cut slide 12 (MCP) to one sentence and
show only one of slides 13 and 14; tutorial 3 covers both.

## 14 to 24 min. A worked example from our own practice

**Slides**

17. Three ways to run the same agent: terminal, editor, headless ([](#fig-agents-three-modes)).
18. Claude Code in the terminal, on this repository.
19. The editor integration. TODO: screenshot.
20. Headless: `claude -p` on cascadia, one GitHub issue at a time.
21. The minimal-change rule. TODO: the exact wording.
22. The run that looped, and how it was killed. TODO: the log excerpt.
23. What the three modes are each for.

```{figure} ../../img/agents-three-modes.svg
:name: fig-agents-three-modes
:width: 100%

The same agent, three ways to run it. The red loop in the headless lane is the run that
had to be killed.
```

**Speaker notes**

The examples in this segment are the group's own. Two are documented; one is not yet.

*Claude Code in the terminal.* The interactive mode. You open a shell in the repository,
the agent reads `CLAUDE.md`, and you converse. Its tool calls appear inline and each
permission-gated action asks before it runs. This is the mode for exploratory work and for
anything where you want to watch. The June blog post [@denolle2026three] describes three
research experiments run this way: a theory-connecting exercise on coupling in seismic
velocity monitoring, a parameter-space scan across roughly 110 monitoring papers to
quantify epistemic uncertainty from analyst choices, and a cross-disciplinary study of
river braiding at Mount Rainier run end to end with Claude Code, with a geomorphologist as
a real collaborator rather than a reviewer of record. The first of those carried an
Anthropic bill of about 200 dollars a month; the post gives that number and the
reasoning behind it.

Demo plan for slide 16: open Claude Code in this repository, ask it to list the pages
under `book/chapters/` that have no front matter, and let the audience watch it read
`AGENTS.md`, glob the directory, read the heads of a few files, and answer. Two minutes.
The point is the sequence of tool calls, not the answer.

*The editor integration.* The same agent inside VS Code, with the diff shown in the
editor and the conversation in a side panel. Same context file, same tools, same
permission model. What changes is the affordance: you see the edit before it lands.
TODO: one screenshot from a real session on this repository, and one sentence on what the
integration does and does not share with the terminal session (conversation history,
permissions, settings). Verify against the current documentation before stating it.

*Headless.* The non-interactive mode. `claude -p` takes a prompt, runs the loop without a
terminal, and exits; its output can be captured as text or as structured JSON. The
practice this segment is named for ran that mode on the cascadia machine: a scheduler
pulled open GitHub issues from a repository, handed each one to a headless run with a
standing instruction to make the smallest change that resolves the issue and to open a
pull request, and moved to the next. One issue per run, one run per issue.

TODO, all of the following, from the actual logs and scripts, none of it from memory:

- Which repository and which issues (numbers, titles, dates).
- The exact wording of the minimal-change rule and where it lived (context file, prompt,
  or both).
- The scheduler: a cron entry, a shell loop, a GitHub Action, or a hand-started script.
- The permission mode and tool allow-list the runs used.
- The run that looped: what the loop was (the agent re-reading the same file, re-running a
  failing test, or re-opening the same PR), how long it ran, what it cost, how it was
  detected, and who killed it and how. Include the log excerpt if it exists.
- Whether the killed run left a partial branch or PR behind, and what was done with it.

Until those are filled in, present slide 20 as a story told from memory and say so.

*What each mode is for.* Terminal for exploration and anything you want to watch. Editor
for changes you want to review line by line as they happen. Headless for work that is
well specified, repeated, and bounded: the issue queue, the nightly review, the
scheduled report. The looping run is the argument for "bounded": a headless run needs a
turn limit, a cost limit, or a wall-clock limit, and preferably all three, because there
is no person in the loop to notice.

**Timing.** Ten minutes is right if the demo is rehearsed and the headless story is
told, not read. If the TODOs above are not filled by lecture day, the headless part will
be thin, and the segment could shrink to seven minutes.

## 24 to 33 min. Building one small skill, live

**Slides**

24. What we are building: a skill that inserts the agreed NSF acknowledgement
    ([](#fig-agents-skill-flow)).
25. The skill format, from `.claude/skills/README.md`.
26. Live: write `SKILL.md`, invoke it, watch it trigger on its own.
27. What a skill is good for and what it is not.

```{figure} ../../img/agents-skill-flow.svg
:name: fig-agents-skill-flow
:width: 100%

How the skill runs: the description is matched, the body loads, the wording is read from
the governance page and inserted verbatim. The amber branch is the failure to rehearse.
```

**Speaker notes**

We build a skill that solves a real, small problem in this repository. The governance page
[How we work](../../governance/how-we-work.md) carries the acknowledgement wording for the
three linked NSF awards and requires it to be used verbatim wherever it is cited;
`AGENTS.md` repeats that rule. An agent asked to "add the NSF acknowledgement" to a page
will, left to itself, paraphrase. A skill fixes that.

The format, from this repository's own skills README: a folder under `.claude/skills/`
containing a `SKILL.md` with `name` and `description` in the front matter and the
instructions below. The description is what the harness matches against; the body is
what it loads.

Live steps, in order:

1. Create `.claude/skills/nsf-acknowledgement/SKILL.md`.
2. Front matter: name `nsf-acknowledgement`; description along the lines of "Insert the
   agreed GAIA acknowledgement wording for the NSF awards, verbatim, when a page, talk,
   poster, or paper needs an acknowledgement or funding statement."
3. Body: three instructions. Read the wording from
   `book/governance/how-we-work.md` rather than from this file, so there is one source of
   truth. Insert it verbatim, including all three award numbers. If the seed grant or the
   Paros Center contributed, append the second sentence from the same page.
4. Save. Ask the agent, in a fresh session, to "add a funding acknowledgement to
   `book/chapters/mt-rainier.md`". Watch whether the skill triggers without being named.
5. Then invoke it by name and compare.

What to say while typing. The body reads the source page instead of embedding the
wording: that is the difference between a skill and a snippet. A snippet goes stale. The
description is short because the harness only sees descriptions at startup, and a long
one is paid for on every session. The three instructions are imperative and specific;
a skill that says "be careful with acknowledgements" would load and do nothing.

What a skill is not. It is not a tool: it cannot execute anything the agent could not
already execute. It is not a subagent: it runs in the parent's context, not a fresh one.
It is a packaged procedure, and its value is that the procedure was written once by
someone who knew the rule.

**Timing.** Nine minutes is generous for a three-instruction skill. Use the surplus to
let the audience propose the second skill they would write, and take two suggestions.
Rehearse step 4; if the skill does not trigger on its own, say why (the description did
not match) and fix the description in front of them. That failure is more instructive
than the success.

## 33 to 40 min. Where agents sit in GAIA, and how we score them

**Slides**

28. The four hubs and the agent layer across them ([](#fig-agents-gaia-layer)).
29. DataHub: the agentic downloader over `gaia-cli`, STAC, and `s3://cresst`.
30. GaiaAgent: translator, downloader, research-software agent.
31. GaiaPilot on LLMaven. TODO.
32. What the CSSI proposal promised: the agent taxonomy and the delivery metrics.
33. How we score an agent: HazEvalHub, the gaia-eval harness, the expert track
    ([](#fig-agents-evaluation)).
34. What we will build first.

```{figure} ../../img/agents-gaia-layer.svg
:name: fig-agents-gaia-layer
:width: 100%

Where the agents sit: three hubs, the GaiaAgent layer over them, HazEvalHub scoring every
agent. The amber box is a name no repository document mentions.
```

**Speaker notes**

*The layer.* The book describes GaiaAgent as the umbrella for the agents that work
alongside the DataHub, ModelHub and HazEvalHub ([GaiaAgent](../../chapters/gaia-agent.md)).
Three are named there: the Gaia Translator, a cross-disciplinary question-answering
assistant grounded in cited cards; the agentic data downloader; and the research-software
agent. The public repositories are `gaia-agentic-ai`, `gaia-translate-QA`, `gaia-skills`
and `gaia-literature-kb`, all under the `gaia-hazlab` organisation.

*DataHub.* The agentic downloader is the clearest case of an agent wrapping existing
tooling rather than replacing it. The book's scaffold page
([Agentic Data Downloader](../../chapters/gaia-agentic.md)) describes it as turning a
natural-language or programmatic request into a staged, provenance-tracked dataset on
`s3://cresst`, by driving `gaia-cli`, the reproducible download recipes in
`gaia-data-downloaders`, and the STAC catalogues for discovery. The agent's job is the
glue and the provenance record; the downloads themselves are the same scripts a person
would run. That page is marked draft, and the open questions and roadmap sections are
empty. TODO: confirm with the DataHub lead what is running today versus planned.

*The translator.* The one with the most design already written down. Three layers:
short per-discipline summaries in the system prompt, a retrieval corpus of cards under
500 words each, and an agent playbook with explicit refusal patterns for forced
analogies and fabricated citations ([Gaia Translator](../../chapters/gaia-translator.md)).
It is also the one with an expert-evaluation plan, which makes it the natural first
subject for tutorial 4.

*GaiaPilot on LLMaven.* TODO. Neither name appears anywhere in this repository, in the
book, or in the coordination documents. Before this slide can be written we need: what
GaiaPilot is (a product, a deployment, an agent), what LLMaven is (a platform, a model
server, a vendor), who operates it, whether it is running today, and where it is
documented. Until then the slide is a title and a question to the room.

*What the CSSI proposal promised.* The proposal itself is not in the repository; the
raw documents are git-ignored by design. What is public is the derived plan in
`project_coordination/03-ai-tools-and-evals.md`, which lists five agent classes: a
research-software-engineering agent that scaffolds repositories, writes tests and
containerises; data-interrogation agents that query the DataHub and build AI-ready
cubes; modelling and surrogate agents that train, evaluate and emit model cards;
coordination agents that draft meeting summaries and metrics narratives; and the
translator. The same document names three CI-template repositories for year one,
including `gaia-template-agent`, a research agent shipped with an evaluation harness and
a provenance YAML. The delivery commitments those templates count against are the D1 to
D5 rows on the [How we work](../../governance/how-we-work.md) page. TODO: check the
agent list and the year-one numbers against the funded proposal text, and note any
agent promised there that the derived plan dropped.

*How we score an agent.* Evaluation is a CSSI deliverable, so it gets its own slide here
rather than a mention at the end. The pattern is already live for agent tasks in the
HazEvalHub prototype and is written into the coordination plan
(`project_coordination/03-ai-tools-and-evals.md`, sections 3 and 4). A task ships with a
public train and validation split and a hidden test split. The agent under test runs in
four conditions: local or cloud model, with or without domain skills. The `gaia-eval`
harness runs it in its container against a declarative JSON scoring spec and emits a
scorecard: is it right, what did it cost, is it reproducible, plus provenance (model card
version, data DOIs, container digest). Three things consume the scorecard: the board, where
each model appears twice and the line between the hollow and filled markers is the skill
lift; a CI regression gate, so a pull request that lowers skill fails; and the Metrics
Observatory. A second track, the translator's expert evaluation with an 8-criterion rubric
and 15 to 25 domain reviewers, answers a different question: not how the model behaves on
held-out data, but whether the answer is useful to a researcher
([Gaia Translator](../../chapters/gaia-translator.md)). The phasing in the plan: v0 live
now with agent tasks; v0.5 in year one adds the first hazard task on the same board; v1 in
year two adds containerised submissions and auto-scoring; v2 in year three fills the pillar
by hazard grid. The research-workflow track in the task taxonomy scores trajectories, which
is the bridge to the last segment.

```{figure} ../../img/agents-evaluation.svg
:name: fig-agents-evaluation
:width: 100%

How GAIA scores an agent: hidden test split, four conditions, the gaia-eval harness, a
three-question scorecard, and its three consumers. The expert track answers a different
question.
```

*What we will build first.* TODO: the lead PI's ordering. A defensible default from the
documents above is the `gaia-template-agent` repository, because every other agent is
supposed to be an instance of it and because it is the unit the D1 metric counts.

**Timing.** Seven minutes for seven slides is tight, and two of the slides are TODOs.
The evaluation slide is the one to protect: it is a funded deliverable and it sets up the
tutorial the group will spend the most time on. If GaiaPilot is still unresolved on
lecture day, drop slide 31 and give the time to slides 32 and 33.

## 40 to 45 min. Guardrails, trajectory capture, cost, reproducibility

**Slides**

35. Guardrails we already have written down ([](#fig-agents-trajectory)).
36. Trajectory capture: the options, and the open question.
37. Cost: the one number we have, and the axis HazEvalHub already scores.
38. Reproducibility: the statement we owe every paper.

```{figure} ../../img/agents-trajectory.svg
:name: fig-agents-trajectory
:width: 100%

From a run to a reproducibility statement, and the gap in the middle: the capture mechanism
is not yet chosen.
```

**Speaker notes**

*Guardrails.* Three are already in the coordination plan
(`project_coordination/03-ai-tools-and-evals.md`, section 6): a human approves anything
outward-facing, agents draft it; no fabricated DOIs or citations, provenance must be real;
secrets stay in GitHub Secrets and never in a repository. Two more come from practice.
The minimal-change rule from segment 14 to 24: the smallest diff that resolves the issue,
nothing opportunistic. And bounded runs: a turn limit, a cost limit, and a wall-clock
limit on any run nobody is watching. The June post adds a structural one: agents that
critique other agents, with the critique written to the record
[@denolle2026three].

*Trajectory capture.* This is the open question, and it should be presented as one. A
trajectory is the full sequence of prompts, model outputs, tool calls and tool results
for one run. We need it for three reasons: to audit what an agent did, to score agents
on process rather than only on outcome (the "research-workflow" track in HazEvalHub's
task taxonomy already calls this trajectory scoring), and to reproduce a run. The
candidate mechanisms, none of them verified for our setup:

- The harness's own session log. Claude Code writes a transcript of each session to a
  local directory. TODO: verify the location, format and retention for the current
  version, and whether headless runs write one.
- Structured output from headless runs. `claude -p` can emit JSON rather than plain text,
  which would capture the final result and some metadata. TODO: verify what is included
  and whether intermediate tool calls are.
- An evaluation harness that wraps the agent and records everything, which is what the
  FrugalMind prototype does for its scored tasks. TODO: confirm which harness it uses
  and whether it can wrap an interactive Claude Code session or only a headless one.
- A proxy in front of the model API that logs every request and response. Complete, but
  it captures the vendor's wire format rather than the agent's semantics.

The decision to make, and it belongs to the group: which of these we adopt as the
default for GAIA agents, where the trajectories are stored, and who may read them.

*Cost.* Two facts and one design choice. The fact from practice: one of the three
experiments in the June post ran at about 200 dollars a month in API charges
[@denolle2026three]. The fact from evaluation: HazEvalHub's prototype scores every
submission on three questions, whether it is right, what it cost, and whether it is
reproducible, and plots cost against performance so that a cheap model that reaches the
same score wins ([HazEvalHub](../../chapters/hazevalhub.md)). The design choice that
follows: cost is a first-class axis for every agent we ship, recorded per run and
reported next to accuracy. The early result on that board, that free local 7 billion
parameter models match cloud models on configuration tasks once given domain skills but
not on numerical code generation, is the reason to keep open-weight models in the
comparison rather than assume the vendor model. TODO: per-participant and per-run costs
for the tutorials, in the [compute requirements](compute-requirements.md) document.

*Reproducibility.* The June post argues that if an agent cannot reproduce your work,
neither can your collaborators, and proposes a Reproducibility Statement in every paper
alongside the data availability statement [@denolle2026three]. The coordination plan
already requires a provenance YAML and an RO-Crate with every agent and publication
(`project_coordination/03-ai-tools-and-evals.md`, section 1.2). What we do not yet have
is the link between the two: a trajectory in the provenance record. That is the
concrete thing this lecture should leave the team wanting to build, and it is the
subject of [tutorial 4](tutorials/04-evaluating-agent-runs.md).

**Timing.** Five minutes for four topics is one and a quarter each. It works only if
trajectory capture is presented as a question and not answered. Leave no time for
questions here; the tutorials are where the discussion goes.

## Mirrored source: the eScience workshop

The UW eScience Institute ran *Coding with AI Agents: A Hands-On Workshop for
Researchers* on 17 June 2026, 9:00 to 12:00, in the Hans Rosling Center, Room 155
[@escience2026codingagents]. The event page, fetched on 17 September 2026, describes the
workshop as a practical introduction to AI coding agents for research software
engineering: an overview of the ecosystem, how language models support tool calling and
code generation, a live demonstration of an agent executing a plan-implement cycle, and a
hands-on exercise building a simple agent. The stated learning outcomes are that
participants can articulate what a coding agent does, evaluate and configure agents for
research contexts, identify limitations and failure modes, and apply agent workflows to
research software tasks. Prerequisites: no background in AI or machine learning; a laptop
with Google Chrome and a GitHub account.

That arc maps onto this lecture as follows. Their ecosystem overview is segments 0 to 14.
Their live demonstration is segment 14 to 24. Their hands-on exercise is segment 24 to
33 and tutorial 2. What this lecture adds is the GAIA-specific material in segments 33
to 45 and the evaluation tutorial.

TODO: the event page names no instructors and links no materials or repository. Ask the
eScience contacts for the slides, the exercise, and the names to credit, and add them
here before this section is presented as a mirror rather than a summary of a listing.

## Sources used, and what is still missing

Verified and used on this page:

| Source | What was taken from it |
|---|---|
| This repository (`AGENTS.md`, `CLAUDE.md`, `.claude/skills/README.md`, `.claude/agents/`, `.claude/gaia-review-personas/shared/`) | Context-file example, skill format, persona subagents, the shared rubric and method, the synthesis step |
| [How we work](../../governance/how-we-work.md) | Award numbers, verbatim acknowledgement rule, D1 to D5 delivery rows |
| [HazEvalHub](../../chapters/hazevalhub.md) | Three scoring questions, cost axis, local 7B model result, trajectory-scoring track |
| [GaiaAgent](../../chapters/gaia-agent.md), [Agentic Data Downloader](../../chapters/gaia-agentic.md), [Gaia Translator](../../chapters/gaia-translator.md) | Agent layer, DataHub wrapping, translator architecture |
| `project_coordination/03-ai-tools-and-evals.md` | Agent taxonomy, template repos, guardrails, provenance requirement, the evaluation harness and its phasing |
| Blog index and two posts [@denolle2026blog; @denolle2026beforeprompt; @denolle2026three] | Three layers, three experiments, 200 dollars a month, reproducibility statement |
| eScience event page [@escience2026codingagents] | Title, date, venue, description, outcomes, prerequisites |

Not found in any source and left as TODO on this page:

- The cascadia headless runs: repository, issues, scheduler, minimal-change rule text,
  permission mode, and the looped run's log.
- The VS Code integration: screenshot and a verified sentence on what it shares with the
  terminal session.
- GaiaPilot and LLMaven: definitions, operator, status, documentation.
- The funded CSSI proposal's own list of promised agents, to check against the derived
  plan.
- The order in which agents will be built.
- The trajectory-capture mechanism: location and format of harness session logs, headless
  JSON output contents, which evaluation harness FrugalMind uses.
- A citation for position effects in long contexts.
- A link to the MCP specification.
- eScience workshop instructors and materials.
