runtime: python311

entrypoint: gunicorn -b :$PORT app:server --workers 4 --timeout 120

env_variables:
  DATA_SOURCE_URL: "https://gaia-hazlab.github.io/website/data"
  # MAPBOX_TOKEN: "your_mapbox_token_here"  # Optional: Uncomment and add token for satellite layers

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 10

instance_class: F2
