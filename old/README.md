# Archived / unused files

The book is built with **MyST** (`jupyter book build` v2, driven by the root `myst.yml`).
Files here are not used by that build and are kept only for reference.

- `book/_config.yml` — legacy Jupyter Book **v1** configuration, superseded by `myst.yml`.
